groups:
  - name: master
    jobs:
      - development
      - qa
  - name: pull-request
    jobs:
      - dataworks-aws-collections-rehydration-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
