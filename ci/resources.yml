resources:
  - name: dataworks-aws-collections-rehydration-pr
    type: pull-request
    source:
      repository: dwp/dataworks-aws-collections-rehydration
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: dataworks-aws-collections-rehydration
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/dataworks-aws-collections-rehydration.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: dataworks-aws-collections-rehydration-update-pipeline
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/dataworks-aws-collections-rehydration.git
      paths:
        - ci/*
        - aviator.yml
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h 
